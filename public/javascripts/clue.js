function attachcluetopage{
	//get Element we want to attach the clue to
	var clueholder = document.getElementById("clues");
	//check what number clue they are currenty on
	var cluenumber = 1;
	//grab the next clue from the clue array
	//every clue consists of a hint, and an answer
	//set the clue to 
}

function clues(){
	cluenumber = 0;
	clues = [
		{hint:,answer:},
			{}
	]
	return function(){
		if ()
	}
}